////////////////////////////////////////////////////////////
//
// SHOOTER
//
// Autor:       Gerard Wypych
// Prowadzący:  mgr inż. Bartłomiej Gajewski
//
////////////////////////////////////////////////////////////

#ifndef MENU_HPP
#define MENU_HPP

////////////////////////////////////////////////////////////
// Headers
////////////////////////////////////////////////////////////
#include "SFML/Graphics.hpp"
#include "Button.hpp"
#include "SimpleText.hpp"

class Menu
{
    public:
        ////////////////////////////////////////////////////////////
        /// \brief  Default (public) destructor
        ///
        ////////////////////////////////////////////////////////////
        ~Menu(){}

        ////////////////////////////////////////////////////////////
        /// \brief  Initializing the Menu, creating all buttons etc.
        ///
        ////////////////////////////////////////////////////////////
        static bool init( );

        ////////////////////////////////////////////////////////////
        /// \brief  Draw all objects in menu
        ///
        ////////////////////////////////////////////////////////////
        static void draw();

        ////////////////////////////////////////////////////////////
        /// \brief  Handle all actions in menu
        ///
        ////////////////////////////////////////////////////////////
        static void logic();

        ////////////////////////////////////////////////////////////
        /// \brief  Run the Menu
        ///
        ////////////////////////////////////////////////////////////
        //static void run();

        ////////////////////////////////////////////////////////////
        /// \brief  Close the Menu, release memory
        ///
        ////////////////////////////////////////////////////////////
        static void close();

    private:
        ////////////////////////////////////////////////////////////
        /// \brief  Default (private) constructor
        ///
        ////////////////////////////////////////////////////////////
        Menu(){}

        static Menu * p_this; /// < Pointer to the only object of Menu class

        SimpleText title; /// < Text with title of the application

        ////////////////////////////////////////////////////////////
        /// BUTTONS IN MAIN MENU
        ////////////////////////////////////////////////////////////
        Button new_game;
        Button load_game;
        Button highscores;
        Button exit;

        ////////////////////////////////////////////////////////////
        /// \brief Enumeration of Menu status
        ///
        ////////////////////////////////////////////////////////////
        enum Status
        {
            NEWGAME,
            LOADGAME,
            HIGHSCORES,
            EXIT
        };
        Status status; /// < Menu status
};

#endif // MENU_HPP
